// Package app holds vivi's rules about operating a Vault, as plain functions
// over plain values. tui is the driving adapter and vault the driven one; the
// dependency points inward only, and neither may reach for tui.
package app
